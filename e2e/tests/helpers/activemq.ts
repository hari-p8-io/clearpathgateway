// Placeholder for ActiveMQ helper. For full e2e, add a STOMP client
// like 'stompit' or 'webstomp-client' to push/consume messages.
// Example API to implement:
// - sendToQueue(queue: string, message: string)
// - consume(queue: string, onMessage: (msg: string) => void)
export {};
